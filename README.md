# LinkSnifferAndroid

Android diagnostic app for authorized traffic inspection using PCAPdroid integration. Filters visible streaming-related endpoints such as `.m3u8`, `.mpd`, `.m4s` and `.ts` without bypassing TLS pinning or forcibly decrypting HTTPS.
